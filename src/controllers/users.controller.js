const AbstractController = require("./abstract.controller")

class UsersController extends AbstractController {
  createEndpoints() {
    // TODO: init routes here via this.router.get(.... etc
  }
}

module.exports = UsersController
